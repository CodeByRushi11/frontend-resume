import React, { Fragment } from "react";

const Column = () => {
  //   const items = [];
  return (
    <Fragment>
      {/* {items.map((item) => {
        <Fragment key={item.id}>
          <h1>Title</h1>
          <h1>{item.title}</h1>
        </Fragment>;
      })} */}
      <td>Name</td>
      <td>Vishwas</td>
    </Fragment>
  );
};

export default Column;
